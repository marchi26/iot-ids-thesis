# Data Directory

`raw/` is reserved for downloaded TON_IoT / UNSW dataset files.

`processed/` is reserved for optional processed files generated by the preprocessing pipeline.

Dataset files are excluded from Git. Do not commit raw datasets, processed large files, or generated artifacts that cannot be reproduced from the code.

Expected dataset location:

```text
data/raw/train_test_network.csv
```
